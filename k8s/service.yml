apiVersion: v1
kind: Service
metadata:
  name: online-shop-service
  namespace: online-shop-prod
spec:
  type: NodePort
  selector:
    app: online-shop
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30008
